#!/usr/bin/env ruby -wKU
require "rubygems"
require "bundler/setup"

$LOAD_PATH.unshift(File.dirname(__FILE__) + '/../lib')

require 'international_trade'
app = InternationalTrade::App.new(ARGV, STDIN)
app.run
